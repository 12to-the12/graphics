import numpy as np


def arr(x):
    return np.array(x)


world_origin = arr([0.0, 0.0, 0.0])

n_Z = np.array([0.0, 0.0, -1.0])

X = np.array([1.0, 0.0, 0.0])
Y = np.array([0.0, 1.0, 0.0])
Z = np.array([0.0, 0.0, 1.0])
